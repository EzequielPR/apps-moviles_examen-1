import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ezequiel_pr.intro_moviles.examen1',
  appName: 'Apps moviles - Examen #1',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
