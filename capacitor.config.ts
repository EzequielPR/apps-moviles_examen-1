import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'apps-moviles_examen-1',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
